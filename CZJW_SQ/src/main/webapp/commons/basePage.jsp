
     <input type="hidden" id="pageName" value="${pageName}">
     <input type="hidden" id="zddb" value="${zddb}">
     <input type="hidden" id="organId" value="${organId}">
     <input type="hidden" id="fromId" value="${fromId}">
     <input  type="hidden"  id="finalState" name="finalState" value="${finalState}">
     <input type="hidden" id="cluesNum" value="${cluesNum}">
     <input type="hidden" id="cluesId" value="${cluesId}">
     <input type="hidden" id="reflectedName" value="${reflectedName}">
     <input  id="control" value="${control}">
     